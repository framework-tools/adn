{
    "name": "@framework-tools/workspace",
    "description": "Template for setting up a TypeScript monorepo",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "clean": "lerna run clean",
        "build": "lerna run build",
        "pub": "lerna publish",
        "test": "lerna run test",
        "lint": "eslint --ext js,ts,tsx ."
    },
    "devDependencies": {
        "eslint": "~7.25.0",
        "eslint-plugin-import": "~2.22.1",
        "lerna": "^4.0.0",
        "typescript": "~4.2.0"
    },
    "engines": {
        "npm": ">=7"
    }
}
